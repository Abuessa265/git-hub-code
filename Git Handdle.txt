For each new repository
1. git init
2. git add .
3. git commit -m "Any Message"
4. git branch -M master/main
5. git remote add origin https://github.com/Abuessa265/git-hub-code.git
6. git push -u origin master/main


// After changes in existing repository/update or delete aney repository folder er modder folder
1. git add .
2. git commit -m "Any Message"
3. git pull origin main //1,2,4 diye kaj korar por kaj na hole 3 number ta deye then 4 number ta dela kaj hbe
4. git push origin main




Another git user access and pull etc

git clone
git status
git add .
git commit -m "your comment"
git push origin main
git pull

git branch
git pull origin (branch name)
git branch
git checkout (branch name)

// Record in PC
cmd then type: PSR
